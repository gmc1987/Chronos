# IAM 生产核心升级清单

## 本轮已完成

- [x] 保留 `platform-iam` 单一 IAM 边界，不新增门户或认证微服务
- [x] 管理账号增加员工绑定、账号类型、启停、失败次数、定时锁定、强制改密、登录信息和令牌版本
- [x] 登录失败 5 次锁定 30 分钟；成功登录清零；账号停用和锁定在认证链生效
- [x] 修改密码、管理员解锁、强制下线及 Refresh Token 全量撤销
- [x] JWT 写入并校验 `tokenVersion`，支持立即失效已签发访问令牌
- [x] 密码策略升级为至少 10 位，包含大小写字母和数字
- [x] 医院、组织单元、员工、岗位、职称、多组织任职及主任职模型
- [x] RBAC 将角色菜单与角色权限解耦，并兼容迁移原角色-菜单-权限关系
- [x] 角色数据范围：全部、机构、部门、部门及下级、本人、自定义机构/部门/员工
- [x] IAM 管理接口统一方法级权限校验；内置管理员权限自动补齐
- [x] 用户密码哈希和员工证件密文禁止 API 回传
- [x] CORS 支持生产环境变量配置；清除配置文件中的明文外部服务密钥
- [x] 管理端增加员工、岗位、职称维护入口
- [x] 完整组织架构管理：部门树、岗位、独立职级、员工档案、多任职与主任职
- [x] 后端全 Reactor 编译/打包通过，前端生产构建通过

## 后续部署与外部对接

- [ ] 上生产前改用 Flyway/Liquibase 管理数据库版本，关闭 `ddl-auto:update`
- [ ] 配置独立生产密钥：`CHRONOS_JWT_SECRET`、数据库凭据、CORS 白名单；密钥进入 KMS/Vault
- [ ] 接入院内 LDAP/AD/OIDC/CA 和 MFA；接口由 integration-center 实现，IAM 保持账号绑定与授权主数据
- [ ] 接入 HR/HIS 人员与科室主数据同步，增加来源标识、同步批次、冲突处理和离职自动停权
- [ ] 将 `IDataScopeService` 接入 OA、排班、文件、消息等业务查询条件
- [ ] 增加敏感操作双人复核、权限申请审批、临时授权和到期回收
- [ ] 增加 IAM 集成测试、权限越权测试、锁定/并发登录测试和审计留痕验收
- [ ] 对敏感字段接入真正的字段加密组件和密钥轮换，不直接存放前端提交的密文
- [ ] 生产启用 HTTPS、反向代理安全头、限流、登录验证码/风控及监控告警
